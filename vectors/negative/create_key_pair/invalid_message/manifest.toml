name = "Negative: Create Key Pair - Invalid Message"
description = "Tests that Create Key Pair returns Invalid_Message error as per KMIP spec"

[[steps]]
operation = "CreateKeyPair"
request = "step1_createkeypair.json"
assert_success = false
assert_error_contains = "cryptographic algorithm must be specified"
