name = "Negative: Register - Invalid Attribute"
description = "Tests that Register returns Invalid_Attribute error as per KMIP spec"

[[steps]]
operation = "Create"
request = "step1_create.json"
assert_success = false
assert_error_contains = "length"
