name = "Negative: Create - Invalid Message"
description = "Tests that Create returns Invalid_Message error as per KMIP spec"

[[steps]]
operation = "Create"
request = "step1_create.json"
assert_success = false
assert_error_contains = "missing field"
