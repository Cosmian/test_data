name = "Negative: Encrypt - Invalid Message"
description = "Tests that Encrypt returns Invalid_Message error as per KMIP spec"

[[steps]]
operation = "Encrypt"
request = "step1_encrypt.json"
assert_success = false
assert_error_contains = "place holder id"
