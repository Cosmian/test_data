name = "Negative: Hash With Unsupported Algorithm (MD5)"
description = "Tests that hashing fails when using an unsupported algorithm (MD5)"

[[steps]]
operation = "Hash"
request = "step1_hash_md5.json"
assert_success = false
assert_error_contains = "support"
