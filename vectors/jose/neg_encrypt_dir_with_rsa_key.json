{
  "type": "expect_error",
  "description": "POST /v1/crypto/encrypt: alg=dir but kid points to an RSA private key — RSA keys lack the Encrypt usage mask, so the KMIP authorization layer returns 404 (not_found) before the handler runs",
  "endpoint": "/v1/crypto/encrypt",
  "setup_key": {"kty": "RSA", "bits": 2048},
  "body": {
    "kid": "$KID",
    "alg": "dir",
    "enc": "A256GCM",
    "data": "dGVzdA"
  },
  "expected_status": 404,
  "expected_error": "not_found"
}
