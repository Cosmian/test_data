# Test KMS client — HTTPS with a PQC (ML-DSA-44) TLS server certificate.
# Port is patched at runtime by load_client_config().
# accept_invalid_certs is required because the server cert is self-signed.
vendor_id = "cosmian"

[http_config]
server_url = "https://localhost:9998"
accept_invalid_certs = true
