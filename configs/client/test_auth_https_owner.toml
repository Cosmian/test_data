# Test KMS client — HTTPS, no client certificate, no authentication. Owner identity.
# Corresponds to server config test_auth_https.toml (port 12005).

[http_config]
server_url = "https://localhost:12005"
accept_invalid_certs = true
