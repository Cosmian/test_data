# Test KMS client — HTTPS with co3 PEM client certificate. CO3 identity (co3.client@acme.com).
# Cert paths are relative to workspace root, resolved by load_client_config().

vendor_id = "cosmian"

[http_config]
server_url = "https://localhost:10004"
accept_invalid_certs = true
tls_client_pem_cert_path = "test_data/certificates/client_server/co3/co3.client.acme.com.crt"
tls_client_pem_key_path = "test_data/certificates/client_server/co3/co3.client.acme.com.key"
